# Brief 2 — US + Canada Shelter Software Integration Map

Shelter/rescue data “originates operationally” where staff/volunteers **create and maintain the animal record**: intake, daily care notes, medical/behavior updates, foster placement, returns, and outcomes (adoption/transfer/return-to-owner). In North America, that system-of-record is typically a shelter management system (SMS) (e.g., PetPoint, ShelterLuv, ShelterBuddy, Chameleon) or a rescue-focused animal management + syndication hub (RescueGroups). Downstream, organizations **syndicate adoptable pets** to public discovery sites (e.g., Petfinder, Adopt a Pet, 24Petconnect) via API, FTP “AutoUpload,” or vendor-managed imports, so those sites are usually **not** the operational source of truth. citeturn9view0turn31view0turn23view0turn26view0turn5search8

A 2026-specific constraint: Petfinder’s developer API is reported as ending (Dec 2, 2025) in Petfinder support materials (even though those pages are intermittently inaccessible), pushing most shelter-facing integrations toward **software-to-Petfinder imports/FTP** and widgets rather than “pull Petfinder via API.” citeturn29search9

## Ranked platform list

Ranking is based on a blended view of: (a) **observed footprint** in US/Canada (vendor or partner claims and widely-used integration hubs), (b) **integration surface area** (public APIs, export feeds, syndication reach), and (c) **connector feasibility** for a 2026 MVP (documentation availability, incremental sync support, and setup friction).

| Rank | Platform | Where it’s most common | Evidence of footprint / ecosystem centrality | Why it matters for “where data originates” | Connector feasibility (one connector per platform) |
|---:|---|---|---|---|---|
| 1 | entity["organization","RescueGroups.org","animal welfare nonprofit"] | Rescues; also some shelters using RG as their posting/syndication hub | Claims **6,200+ partner organizations** and **exports to 200+ adoption websites/services** (including major destinations). citeturn9view0 | For many rescues, RescueGroups is effectively the operational system-of-record for adoptable listings (and sometimes broader workflows) and a major syndication hub. citeturn9view0turn6view0 | **High**: public HTTP/JSON + REST (legacy) and FTP download options are documented; explicit “updatable data API.” citeturn6view0turn7view0 |
| 2 | entity["company","PetPoint","animal shelter software"] | Large shelters, humane societies; North America | Best Friends states PetPoint releases aggregated data on **~1,200 shelter partners**. citeturn35view0 | Strong candidate for “true operational origin” in municipal/high-volume shelter workflows (intake → outcome). citeturn35view0turn34view0 | **Medium**: integrations exist (imports + “webservices”/online listings), but developer-grade API docs are not broadly public; enablement may require vendor involvement. citeturn23view0turn24view0turn5search5 |
| 3 | entity["company","ShelterLuv","animal shelter software"] | Shelters + rescues; modern cloud-first | HumanePro bio for founder says it’s used by organizations in **all 50 states**. citeturn36search1 | ShelterLuv is frequently the record-of-truth where day-to-day animal data (including behavioral attributes) is updated; it also pushes to aggregators. citeturn32view1turn31view0turn20view0 | **High**: explicit “API keys provide access to pull data,” plus documented attribute model aligned to Petfinder/Adopt a Pet; API spec link is provided upon key issuance. citeturn20view0turn32view1 |
| 4 | entity["company","Petstablished","animal welfare software"] | Rescues and smaller-to-mid shelters | Claims **1,000+ animal welfare organizations**; explicit automatic exports to Petfinder + Adopt a Pet + RescueGroups syndication. citeturn17view0 | When used, Petstablished is likely the operational source-of-truth for those orgs’ animals and workflows (apps, fosters, medical records), and it syndicates listings. citeturn17view0 | **Medium–High**: has “iFrame & APIs” plus FTP AutoUpload patterns; public field-level API docs are not prominent, so discovery + mapping work is required. citeturn17view0turn26view2 |
| 5 | entity["company","ShelterBuddy","animal shelter software"] | Shelter operations; municipalities and multi-site orgs | Vendor site claims **200+ shelters**, 1MM+ animals/year. citeturn10view0 | Strong operational system-of-record, and unusually strong developer surface (API incl. incremental sync patterns + behavior assessment resources). citeturn11view0turn16view1turn16view2 | **High**: public API docs + demo Swagger-like docs; explicit incremental polling (`UpdatedSinceUtc`) and behavior assessment endpoints. citeturn11view0turn14view0turn16view1 |
| 6 | entity["company","Chameleon","animal shelter software"] | Municipal/complex operations; field services + shelter | 24Pet positions it as “transformative shelter management” with behavior notes and “pet behaviors” insights; long-tenured municipal usage is highlighted. citeturn33view0 | Likely a “deep operations” source-of-truth in enterprise animal control/sheltering, but data extraction often leans on reporting exports and vendor workflows. citeturn33view0turn18search4 | **Medium–Low**: Adopt a Pet import exists (vendor-managed), but open developer APIs are not clearly public; SAC describes a Crystal Report + SFTP path. citeturn26view0turn18search4 |
| 7 | entity["company","Shelter Pro","animal shelter software"] | Animal control agencies; legacy use cases | Adopt a Pet documents FTP AutoUpload setup requiring in-software mapping (“web uploads”). citeturn26view3 | Operational origin for some agencies, but integration typically depends on file exports and local configuration. citeturn26view3turn28search3 | **Medium–Low**: feasible via file-based ingest; harder to generalize due to on-prem variants + mapping tables. citeturn26view3 |
| 8 | entity["company","Animal Shelter Manager / ShelterManager.com","open source shelter software"] | Long tail; small orgs; cost-sensitive operations | Listed by Adopt a Pet as an importing software option; ASM docs describe “internet publishers” configuration for publishing adoptables. citeturn27view0turn19search19 | Often the operational store for small orgs; publishing/export is a core pattern. citeturn19search19turn27view0 | **Medium**: open documentation and export workflows exist, but deployments vary and may require multiple connector modes (DB, CSV, FTP). citeturn19search19turn27view0 |

Broader long-tail platforms that appear in 2025–2026 Adopt a Pet import guidance include AnimalsFirst, BARRK, Companion Connect, Pawlytics, PetBridge, Multiop, Trackabeast, and RescueConnection (sometimes via RescueGroups as the portal). citeturn27view0turn22search13

## Integration capability matrix

Legend: **API** = documented programmatic interface for data pull/push; **Export feed** = FTP/CSV/SFTP/report extracts; **Partner integrations** = named third-party services (microchip registries, analytics, etc.); **Listing syndication** = automated posting to public adoption sites.

| Platform | Public / documented API for integrators | Export feeds (FTP/SFTP/CSV/reporting) | Partner integrations (examples) | Listing syndication patterns observed | “One connector per platform” practicality |
|---|---|---|---|---|---|
| RescueGroups.org | Yes: HTTP/JSON API; REST API noted (deprecated); developer guide describes multiple access modes incl. FTP downloads. citeturn6view0turn7view0 | Yes: “Complete Adoptable Pet Data Download via FTP” is explicitly represented in the developer guide navigation. citeturn7view0 | Strong ecosystem orientation; also offers iFrame as alternative. citeturn6view0turn7view0 | Explicitly exports to **200+ adoption websites/services**; “update one list, others updated.” citeturn9view0 | Strong: stable docs + predictable auth + broad coverage; best “first connector” for adoptable inventory distribution. citeturn6view0turn9view0 |
| ShelterBuddy | Yes: public API docs show endpoints, rate limiting, and incremental polling (`UpdatedSinceUtc`), plus a full demo documentation endpoint. citeturn11view0turn14view0 | Yes: API supports listing and pagination; also supports pulling PDFs (medical history) via endpoints (indicative of rich export). citeturn11view0 | Vendor advertises “API integration” and partners; also offers public site via API/iFrame. citeturn5search7turn5search19 | Adopt a Pet documents ShelterBuddy AutoUpload via FTP and notes default behavior around values like color/size. citeturn26view1 | Strong: documentation is unusually accessible; incremental sync design reduces operational load for MVP. citeturn11view0turn14view0 |
| ShelterLuv | Yes (keyed): ShelterLuv publishes an API key request process stating keys enable pulling data and are used for custom website displays; API spec is shared by link after key issuance. citeturn20view0 | Yes: for some partner workflows, ShelterLuv uses non-API file transfer (example: 24Petwatch microchip via FTP). citeturn21view1turn20view0 | Many named integrations are enumerated in the API key request (e.g., Adopt a Pet, SPDA, Chewy, Petco Love Lost, Shelter Animals Count, microchip registries). citeturn20view0turn32view1 | Adopt a Pet uses **ShelterLuv API** import; ShelterLuv documentation ties “fixed attributes” directly to Petfinder/Adopt a Pet uploads. citeturn31view0turn32view1 | Strong: one connector can be standardized around an account-scoped API key + consistent attribute model. citeturn20view0turn32view1 |
| PetPoint | Not clearly public, but “webservices” are referenced as a dependency for website iframes, and integrations require enabling/keys. citeturn24view0turn5search5 | Yes: Adopt a Pet integration is vendor-activated; PetPoint’s integration guide references IFrame/Webservices publishing checkboxes and online listing configuration. citeturn23view0turn24view0 | 24Pet describes native connection to 24Petwatch microchip registry. citeturn34view0 | Adopt a Pet import exists; workflow references Petango/online listings and updates “within a few hours.” citeturn23view0turn23view1 | Medium: very valuable footprint, but connector likely needs vendor-supported access patterns and careful mapping of stages/sites. citeturn23view0turn25view2 |
| Petstablished | Mentions “iFrame & APIs” and a “customizable API” (public-facing), but field-level docs are not prominent on the marketing site. citeturn17view0 | Yes: Adopt a Pet describes FTP AutoUpload config stored inside Petstablished (“exports and iframes”). citeturn26view2 | Built-in exports to Petfinder + Adopt a Pet + via RescueGroups are explicitly marketed. citeturn17view0 | Automatic export to Petfinder/Adopt a Pet; plus “through RescueGroups… export to over 200 sites.” citeturn17view0 | Medium–High: feasible, but confirm API scope (adoptable-only vs full operations) early; FTP export is a practical fallback. citeturn17view0turn26view2 |
| Chameleon | API for third parties is not clearly public; integrations appear to be vendor-managed and/or reporting-based. citeturn26view0turn18search4 | Yes: SAC indicates Chameleon users can submit via **custom Crystal Report + secure SFTP**. citeturn18search4 | 24Pet highlights built-in adoption support, 24Petwatch connectivity, and data mapping to “connect data sources.” citeturn33view0 | Adopt a Pet: Chameleon import can be enabled (updates **hourly**) and references presence on 24Petconnect as a precursor signal. citeturn26view0turn5search8 | Medium–Low: often requires coordination with vendor/support; standardized connector likely needs an “export/report ingest” mode. citeturn18search4turn26view0 |
| Shelter Pro | No clear public API in the cited setup docs; integrations center on “web uploads” and value-mapping. citeturn26view3 | Yes: Adopt a Pet setup requires entering provided FTP credentials and configuring “web uploads” inside Shelter Pro. citeturn26view3 | Not emphasized in sources; typically more self-managed integrations. citeturn28search3turn26view3 | Adopt a Pet FTP AutoUpload; mapping tables are explicitly part of setup. citeturn26view3 | Medium–Low: feasible as batch ingestion, but high variability across deployments and mapping values. citeturn26view3 |
| Animal Shelter Manager / ShelterManager.com | Not positioned as a public API platform in sources; strength is documented “internet publishing” workflows. citeturn19search19 | Yes: publishing settings for internet publishers are documented (publishers, selection rules, bonded animals, etc.). citeturn19search19 | Not emphasized in cited sources. citeturn19search19turn27view0 | Appears in Adopt a Pet software list; implies compatibility with upload pipelines. citeturn27view0 | Medium: “connector” may be multi-modal (file ingest + optional direct DB access) depending on self-hosting patterns. citeturn19search19turn27view0 |

Cross-border note: Adopt a Pet’s PetPoint import guide states PetPoint IDs start with **US or CA** country code, evidencing a shared US/Canada operational footprint for that platform family. citeturn23view0

## Field availability matrix

Focus: temperament/behavior fields (structured vs free-text). The practical requirement for a unified integration layer is to support both: (a) **structured “signals”** (booleans/enums) and (b) **free-text notes** (kennel card/bio/behavior notes), plus optional “assessment” documents.

| Platform | Structured temperament/behavior fields | Free-text behavior/temperament notes | Implications for a canonical model |
|---|---|---|---|
| RescueGroups.org | Strongly structured: object definitions include many enumerated behavior/environment fields (e.g., energy level, exercise needs, housetrained, reaction to new people, OK with cats/dogs/kids, affectionate, timid/skittish, etc.). citeturn8view0 | Yes: includes description fields (`animalDescription`, `animalSummary`, etc.) that can carry narrative context. citeturn8view0 | Map into a rich schema: `behavior_traits[]` (enums/booleans) + `behavior_notes` (text) + `environment` + tolerance for “unknown.” citeturn8view0 |
| ShelterBuddy | Structured behavior subsystem exists: `Behaviour` appears as a nested object, plus dedicated endpoints for behaviour assessment list and behaviour status lists. citeturn16view0turn16view1turn16view2 | Yes: behaviour assessments include a free-text `Notes` field in addition to the assessment text/label. citeturn16view1 | Model should support both “assessment events” (repeatable) and point-in-time flags; design for incremental sync and high cardinality notes. citeturn11view0turn16view1 |
| ShelterLuv | Explicit behavioral attribute model: **Fixed Behavioral Attributes** and **Custom Behavioral Attributes** exist; fixed attributes “correlate directly” with Petfinder/Adopt a Pet uploads. citeturn32view1 | Yes: each attribute supports “stored text” for extra context (per animal), and “Website/Kennel Card Memo” can serve as narrative in downstream listings (as described by Adopt a Pet import behavior). citeturn32view1turn31view0 | Canonical model should treat behavioral attributes as first-class structured tags, with optional per-tag text, plus a general narrative description. citeturn32view1turn31view0 |
| PetPoint | Structured “Good with Cats/Dogs/Kids” and “Housetrained” are explicitly part of data sent to Adopt a Pet. citeturn25view0turn25view2 | Yes: “Petango Adoption Description Memo” is exported as listing description; UI guidance also points to an Animal Profile tab for behavior info, implying narrative capture. citeturn25view0turn25view2 | Treat as mixed: structured compatibility flags + narrative memos; connector must normalize stages/sites, and preserve both “what” and “why.” citeturn23view1turn25view2 |
| Petstablished | Not explicitly enumerated in accessible docs, but exports to Petfinder/Adopt a Pet/RescueGroups imply support for the common fixed attributes required by those feeds. citeturn17view0 | Likely yes (bios/notes are common in listing exports), but public documentation in the cited sources does not enumerate specific behavior fields. citeturn17view0turn26view2 | For MVP, plan discovery work: confirm which behavior flags are supported in the “customizable API” vs only in export feeds; schema should allow partial mapping. citeturn17view0turn26view2 |
| Chameleon | 24Pet marketing/testimonials describe “pet behaviors” insights and explicitly mention “entering behavior notes.” citeturn33view0 | Yes (explicit “behavior notes” in testimonial). citeturn33view0 | Expect heavy use of free-text and custom fields; MVP mapping should focus on a minimal common core + preserve raw notes verbatim. citeturn33view0turn18search4 |
| Shelter Pro | Adopt a Pet setup docs focus on upload config and mapping values; behavior-specific field list is not surfaced in those docs. citeturn26view3 | Likely (most systems support a free-text description), but not demonstrated in cited sources for behavior fields specifically. citeturn26view3turn28search3 | Treat as a “batch export” connector with conservative expectations: capture description reliably; add structured traits only when verifiably available. citeturn26view3 |
| Animal Shelter Manager / ShelterManager.com | Publishing settings are richly documented (what counts as adoptable, bonded animal handling), but behavior field structure is not enumerated in the cited page. citeturn19search19 | Commonly relies on notes/bio fields for publisher outputs (implied by publisher configuration patterns), but behavior structure is not specified in the cited sources. citeturn19search19turn27view0 | Expect variability: support “best-effort structured” + always store the raw narrative and raw source payload for forward compatibility. citeturn19search19 |

Practical takeaway: the ecosystem mixes (a) **fixed, standardized behavioral attributes intended for aggregators** (very explicit in ShelterLuv; and visible in PetPoint → Adopt a Pet exports), (b) **customizable attributes/tags**, and (c) **high-value free-text notes** (behavior notes, kennel card memos), which are often the only place nuanced temperament information is recorded. citeturn32view1turn25view2turn31view0turn33view0

## Recommended integration order for an MVP

An MVP that aims to “find where the data originates” should prioritize connectors that (1) attach to the **system-of-record** in real shelter operations, (2) have a **documented, automatable extraction surface**, and (3) cover a **large share of organizations** with minimal per-shelter customization.

1) **RescueGroups connector (adoptable inventory + org metadata first)**  
This captures a very large rescue segment quickly (6,200+ partner orgs) and provides a well-described API surface plus FTP download options. It also provides syndication leverage (exports to 200+ sites), making it a strong “ecosystem spine” connector for listings. citeturn9view0turn6view0turn7view0

2) **ShelterLuv connector (full animal record + behavioral attributes + notes)**  
ShelterLuv explicitly supports API keys for pulling account data, and its behavioral attribute system is documented in a way that aligns to aggregator exports. This makes it unusually predictable for building a reusable connector that can normalize temperament fields into a canonical model. citeturn20view0turn32view1turn31view0

3) **ShelterBuddy connector (API-first; incremental sync + behavior assessments)**  
ShelterBuddy’s documentation is unusually developer-friendly: it describes incremental polling (`UpdatedSinceUtc`), the “search group” pattern for adoptables, and explicit behavior assessment endpoints (with notes). This reduces operational complexity and supports a strong temperament/behavior representation early in the MVP. citeturn11view0turn16view1turn16view2

4) **PetPoint connector (enterprise shelter footprint; stage/site normalization)**  
PetPoint is a major operational database in North American shelters (Best Friends cites ~1,200 partners). The integration path is more vendor-mediated, and it has multi-site/stage complexity, but the payoff is high because it’s often the true “intake-to-outcome” system-of-record in larger shelters. citeturn35view0turn23view0turn25view2

5) **Petstablished connector (rescue-heavy; validate API scope early)**  
Petstablished claims 1,000+ organizations and offers both exports and “customizable API/iFrame.” For MVP scope control, validate whether the API supports full operational objects (intake/outcome/medical/behavior) versus primarily public adoptable listings; if limited, ingest via export feeds while keeping a roadmap to deeper integration. citeturn17view0turn26view2

6) **Chameleon connector (export/report-based mode, then deepen)**  
Chameleon appears in enterprise municipal contexts and has an Adopt a Pet import path, but extraction is likely to start as report/export ingest (Crystal Reports + SFTP for SAC is explicitly described). Treat Chameleon as a “phase 2” connector with a robust report-ingestion pathway and careful handling of custom fields/behavior notes. citeturn18search4turn26view0turn33view0

7) **Shelter Pro and Animal Shelter Manager (long tail, file-oriented connectors)**  
Both show up in the 2025–2026 Adopt a Pet ecosystem list, but are more likely to require file-based ingestion and per-deployment mapping. These are good “coverage” connectors after the API-first platforms are stable. citeturn27view0turn26view3turn19search19

If the MVP’s goal is explicitly adoptable listings (not full operational data), you can compress the roadmap by prioritizing platforms with strong syndication hubs (RescueGroups first) and “export surfaces” (ShelterLuv/ShelterBuddy/Petstablished), because they minimize implementation variance and credential friction while still getting you close to the operational source for many orgs. citeturn9view0turn20view0turn11view0turn17view0